#ifndef RECURSION_H
#define RECURSION_H

#include "linkedlists.h" // For LogEntry structure

node *reverse_ll(node *head);
int rec_factorial(int n);
int rec_fibonacci(int n);
int max_id(node *head);

#endif // RECURSION_H
